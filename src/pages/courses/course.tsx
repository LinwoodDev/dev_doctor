import React, { ReactElement } from 'react'


export default function CourseRoute(): ReactElement {
    return (
        <div>
            
        </div>
    )
}
export interface CourseProps {
    course: Course;
}