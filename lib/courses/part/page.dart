import 'package:flutter/material.dart';

class PartItemPage extends StatelessWidget {
  final PartItemPage item;

  const PartItemPage({Key key, this.item}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
