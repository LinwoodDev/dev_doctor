import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ServersSettingsPage({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
